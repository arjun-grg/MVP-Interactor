#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
#parse("File Header.java")

import com.hannesdorfmann.${MOSBY_TYPE}.mvp.MvpBasePresenter;

public class ${NAME} extends MvpBasePresenter<${PACKAGE}View>{
}
