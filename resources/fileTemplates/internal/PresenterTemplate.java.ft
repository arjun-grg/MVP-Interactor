#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
#parse("File Header.java")

import com.hannesdorfmann.mosby3.mvp.MvpBasePresenter;

public class ${NAME}Presenter extends MvpBasePresenter<${NAME}View>{
}
